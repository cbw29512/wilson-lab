# Copy this file to terraform.tfvars and replace every example value.
# terraform.tfvars is ignored by Git.

region           = "us-ashburn-1"
compartment_ocid = "ocid1.compartment.oc1..replace-me"
ssh_allowed_cidr = "203.0.113.10/32"
api_domain       = "api.example.com"

# Local Terraform with ~/.oci/config:
oci_auth           = "APIKey"
oci_profile        = "DEFAULT"
ssh_public_key_path = "~/.ssh/id_ed25519.pub"

# OCI Resource Manager alternative:
# oci_auth      = "ResourcePrincipal"
# ssh_public_key = "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... your-name"

# Recommended Always Free starting size.
instance_ocpus       = 1
instance_memory_gbs  = 6
boot_volume_size_gbs = 50

# Change this only when the selected availability domain has no A1 capacity.
availability_domain_index = 0
